(rule
 (target libjson_of_yaml.a)
 (deps
  (glob_files_rec src/*)
  Cargo.toml
  Cargo.lock)
 (action
  (progn
   (run cargo build --release --target-dir target)
   (bash "cp target/release/libjson_of_yaml.a %{target}"))))
