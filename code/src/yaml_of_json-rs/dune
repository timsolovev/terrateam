(rule
 (target libyaml_of_json.a)
 (deps
  (glob_files_rec src/*)
  Cargo.toml
  Cargo.lock)
 (action
  (progn
   (run cargo build --release --target-dir target)
   (bash "cp target/release/libyaml_of_json.a %{target}"))))
